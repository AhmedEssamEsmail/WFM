# Supabase Configuration
# Get these from your Supabase project settings -> API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Test Database Configuration (Supabase Local)
# These are used for running tests against a local Supabase instance
# Start local Supabase with: npx supabase start
# Copy your local keys to .env.test (git-ignored)
VITE_SUPABASE_TEST_URL=http://127.0.0.1:54321
VITE_SUPABASE_TEST_ANON_KEY=your-local-test-anon-key
VITE_SUPABASE_TEST_SERVICE_KEY=your-local-test-service-key

# Sentry Configuration (Optional - for production error tracking)
# Get these from your Sentry project settings
# Leave empty to disable Sentry integration
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
# Options: development, staging, production

# Email Domain Configuration (Optional)
# Restrict signups to specific email domain
# Default: @dabdoob.com
VITE_ALLOWED_EMAIL_DOMAIN=@dabdoob.com
